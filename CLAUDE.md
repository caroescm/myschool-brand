# Code style preferences

Prefer minimal, lazy-but-correct diffs: reuse existing helpers/patterns before writing new ones, avoid speculative abstractions (no interfaces/config for one use case), shortest working change over premature generalization.
